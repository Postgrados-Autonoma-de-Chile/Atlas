node_modules/
.env
.env.local
.data/
.firecrawl/
dist/
*.log
.DS_Store
.claude/
dist

# Salidas del harness pedagógico: se generan una por corrida (npm run eval:tutor).
eval/resultados-*.json

# Texto extraído de los .docx fuente (scripts/extraer-curriculo.py). Regenerable desde el material
# original, que vive fuera del repo — no hay nada acá que no esté ya en curriculo/nivel1.json.
scripts/curriculo/
